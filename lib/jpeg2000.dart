/// Public JPEG 2000 API.
///
/// This library is intentionally byte-oriented and does not expose file paths
/// or `dart:io` types, so it can be imported by Dart VM and browser builds.
library;

export 'src/api/jpeg2000_codec.dart';
